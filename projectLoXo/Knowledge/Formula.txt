begin_formula
	w = sqrt(k / m) 
	w = 2 * 3.14 / T
	T = 2 * 3.14 / w
	T = 2 * 3.14 * sqrt(m / k)
	f1 = 1 / T
	f1 = w / (2* 3.14)
	F = -k * x
	F = -k * delta_l
	F = m * a
	a = F / m
	a = -w^2 * x
	Wd = (1/2) * m * v^2
	Wt = (1/2) * k * delta_l^2
	Wt = (1/2) * k * x^2
	k = 4 * 3.14^2 * m / T^2
        T = sqrt(HeSo1*m1 + HeSo2*m2)
	k1 = k * l / l1
	k2 = k * l / l2
	
end_formula